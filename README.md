# Love-Randomiser
A simple JavaScript project that generates a random love compatibility score between two users based on their names. It uses prompt(), Math.random(), Math.floor(), and conditional statements to create a fun, interactive experience while demonstrating core JavaScript concepts.
